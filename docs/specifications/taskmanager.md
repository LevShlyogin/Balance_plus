# Спецификация продукта «Balance+ Task Manager»

---

**Раздел 1. Общая информация**

| Поле | Значение |
|------|----------|
| Название продукта | Сервис управления задачами «Balance+ Task Manager» |
| Заказчик (отдел/человек) | Отдел инженерных расчётов (БТР, БПР, БВП) / Деминов А.М. |
| Аналитик | Шлёгин Л.Р., Гаврилов П.Я. |
| Дата передачи в разработку | 06.12.2025 |
| Приоритет | Высокий (Ядро платформы) |
| Целевой срок Demo | Q1 2026 |
| Версия | 1.2 |

---

**Раздел 2. Проблема и цель**

*Зачем это нужно? Какую боль решаем?*

> **Проблема:**
> Отсутсвие четкой связи Задача - Расчет - Результат, как результат потеря контекста на дистанции, а также требование вести отдельные сводки по делу проекта для хранения и версионирования данных.
>
> **Цель:**
> Создать "Единое окно" (IDE), которое агрегирует задачи инженера из всех репозиториев, автоматизирует Git-операции (создание веток, коммиты, MR) и предоставляет бесшовный доступ к специализированным расчётным модулям (Штоки, Балансы и др.) без необходимости переключения контекста.
>
> **Критерий успеха:**
> - Время от открытия задачи до начала расчёта < 10 секунд.
> - Ошибки именования веток = 0.
> - Геометрические данные, термодинамические параметры и результаты расчётов (JSON) сохраняются в Git автоматически из интерфейса калькулятора.
> - Частичное управление задачами (перевод в статус «В работе» и «На проверке»)

---

**Раздел 3. Пользователи и роли**

| Роль | Кто это | Что делает в системе |
|------|---------|---------------------|
| Инженер-расчётчик | Сотрудники бюро (БТР, БПР, БВП) | Просматривает свои задачи, создает новые, выполняет расчёты через интегрированные модули, отправляет на проверку. |
| Руководитель бюро | Начальник бюро | Создает задачи, распределяет нагрузку, проводит ревью (Merge Requests) в GitLab. |

---

**Раздел 4. Функциональные требования (Use Cases)**

**UC-01: Просмотр списка задач (Дашборд)**

| Поле | Описание |
|------|----------|
| Актор | Инженер-расчётчик |
| Предусловие | Пользователь авторизован |
| Основной сценарий | 1. Пользователь заходит на главную страницу.<br>2. Система отображает задачи, назначенные на пользователя, из **всех** доступных репозиториев.<br>3. Пользователь фильтрует задачи по Бюро (БТР, БПР, БВП) и конкретному расчетному модулю (напр., "Штоки клапанов").<br>4. Карточка задачи отображает: Название, Проект (имя репозитория), Статус, Срок. |
| Результат | Пользователь видит список актуальных задач. |

**UC-02: Создание новой задачи**

| Поле | Описание |
|------|----------|
| Актор | Инженер / Руководитель |
| Основной сценарий | 1. Нажимает кнопку "+ Новая задача".<br>2. Заполняет Название.<br>3. Выбирает Проект (по наименованию объекта) через поиск с автодополнением по доступным репозиториям.<br>4. Выбирает Тип расчёта (Модуль) из сгруппированного списка.<br>5. Нажимает "Создать". |
| Результат | В выбранном репозитории GitLab создана Issue с метками Бюро и Модуля. **Автоматически создана ветка** для работы. |

**UC-03: Выполнение расчёта (Интеграция приложений)**

| Поле | Описание |
|------|----------|
| Актор | Инженер |
| Предусловие | Задача создана, тип расчёта поддерживается (напр., "Штоки") |
| Основной сценарий | 1. Пользователь кликает на карточку задачи.<br>2.1 Система меняет статус задачи на «В работе».<br>2.2 Система открывает соответствующий расчётный модуль (напр., WSA Calculator) в полноэкранном режиме (Iframe).<br>3. Модуль автоматически подгружает ранее сохраненные данные (Гидрация) из ветки задачи.<br>4. Инженер проводит расчёт.<br>5. Нажимает "Сохранить в Balance+".<br>6. Система перебрасывает пользователя в task manager. |
| Результат | JSON-файлы с входными и выходными данными закоммичены в ветку задачи в папку `calculations/{module}/{timestamp}`. |

**UC-04: Завершение задачи (Submit)**

| Поле | Описание |
|------|----------|
| Актор | Инженер |
| Предусловие | Расчёты выполнены и сохранены |
| Основной сценарий | 1. Пользователь нажимает кнопку "🏁 Завершить" на карточке задачи.<br>2. Система проверяет наличие ветки.<br>3. Система создает Merge Request (MR) в GitLab.<br>4. Выводится уведомление со ссылкой на MR.<br>5.Система меняет статус задачи на «На проверке». |
| Результат | Задача переведена в статус ревью (MR создан). |

---

**Раздел 5. Бизнес-правила**

Это **золотой раздел**. Здесь зафиксирована логика, которую мы реализовали.

| ID | Правило | Пример / Пояснение |
|----|---------|-------------------|
| BR-01 | **Git-Centric Architecture** | GitLab является единственным источником правды. Система не хранит задачи в своей БД, а вычитывает их через API в реальном времени. |
| BR-02 | **Мульти-проектность** | Один инженер может работать с задачами из разных репозиториев (Турбина А, Турбина Б). Привязка к проекту определяется по `project_id` задачи GitLab. |
| BR-03 | **Таксономия Лейблов** | Тип задачи определяется лейблами: `bureau::{код}` (Бюро) и `module::{код}` (Модуль). Отображение на UI (русские названия) маппится через конфиг. |
| BR-04 | **Статусная модель** | Статус задачи определяется лейблом с префиксом `Статус::` (напр., `Статус::В работе`). Цвет статуса жестко задан в справочнике. |
| BR-05 | **Авто-бранчинг** | Ветка создается автоматически при создании задачи или первом клике. Формат имени: `issue/{id задачи}-{транслит названия}`. |
| BR-06 | **Изоляция расчётов** | Внешние сервисы (WSA) открываются в изолированном контейнере, контекст задачи (`taskId`, `projectId`) передается через URL параметры. Обмен данными — через `postMessage`. |

---

**Раздел 6. Данные**

**6.1. Входные данные (откуда берём)**

| Данные | Источник | Формат | Способ получения |
|--------|----------|--------|------------------|
| Список задач (Issues) | GitLab API (Global) | JSON | `GET /issues?scope=assigned_to_me` |
| Список проектов (Repos) | GitLab API | JSON | `GET /projects?membership=true` |
| Файлы расчётов (предыдущие) | GitLab Repository | JSON | `GET /repository/files/...` (из ветки) |
| Справочники турбин/арматуры | Внешний сервис WSA | JSON | Proxy запрос через бэкенд Balance+ |

**6.2. Выходные данные (что отдаём)**

| Данные | Потребитель | Формат |
|--------|-------------|--------|
| Новая Issue | GitLab | API |
| Новая ветка | GitLab | API |
| Результат расчёта | GitLab Repo | Файлы `input.json`, `result.json` |
| Merge Request | GitLab | API |

**6.3. Концептуальная модель данных (Frontend State)**

```
┌─────────────┐       ┌─────────────┐
│   Project   │ 1───* │    Task     │
│ (Git Repo)  │       │   (Issue)   │
├─────────────┤       ├─────────────┤
│ ID          │       │ IID         │
│ Name        │       │ Title       │
│ Web URL     │       │ Description │
└─────────────┘       │ Labels []   │ ───┐
                      │ State       │    │ (Маппинг)
                      │ Due Date    │    ▼
                      └──────┬──────┘ ┌────────────┐
                             │        │  Taxonomy  │
                             │        ├────────────┤
                             │        │ Bureau     │
                             │        │ Module     │
                             │        │ Status     │
                             │        └────────────┘
                             │
                             │ 1───*
                       ┌─────▼───────┐
                       │ Calculation │
                       │  (In Git)   │
                       ├─────────────┤
                       │ App Type    │
                       │ Timestamp   │
                       │ JSON Data   │
                       └─────────────┘
```

---

**Раздел 7. Прототип интерфейса**

**Экран 1: Дашборд (Dashboard)**
*   **Навигация:** Двухуровневая (Вкладки Бюро -> Чипсы Модулей).
*   **Сетка задач:** Адаптивная (`grid`), карточки фиксированной высоты.
*   **Карточка:**
    *   Header: Заголовок + Цветной статус.
    *   Body: Описание (truncate).
    *   Footer: Дата, кнопка "Завершить", Название Проекта (на всю ширину).
*   **Модалка создания:** Поле "Проект" с автодополнением, "Тип расчёта" сгруппирован по Бюро.

**Экран 2: Рабочее пространство (App View)**
*   Полноэкранный режим (перекрывает меню).
*   Контент: Встраиваемый интерфейс (Iframe).
*   Кнопка "Сохранить в Balance+" (инжектится логикой интеграции).

---

**Раздел 8. Нефункциональные требования**

| Требование | Значение | Комментарий |
|------------|----------|-------------|
| Производительность | Загрузка списка задач < 1.5 сек | Используется кеширование проектов на бэкенде |
| UX | Отсутствие горизонтальной прокрутки | `box-sizing: border-box`, адаптивная верстка |
| Интеграция | Поддержка любых веб-приложений | Через Iframe + JS SDK (postMessage) |
| Консистентность | Данные не должны теряться при обновлении | Гидрация данных (Restore State) |

---

**Раздел 9. Открытые вопросы**

| Вопрос | Кто должен ответить | Срок |
|--------|--------------------| -----|
| Формат хранения данных для модуля "Баланс" (он сложнее штоков) | Архитектор | TBD |
| Политика очистки старых веток после слияния | DevOps / Team Lead | TBD |

---

**Раздел 10. Согласования**

| Роль | ФИО | Подпись/дата |
|------|-----|--------------|
| Заказчик | Деминов А.М. | |
| Архитектор | Шлёгин Л.Р. | |

---

### Definition of Ready (Критерии готовности к передаче)

Задача считается выполненной, если:
1.  Спецификация утверждена.
2.  Реализован MVP с поддержкой мульти-репозиториев.
3.  Интегрирован сервис WSA (Штоки).
4.  Проходит полный цикл: Создание задачи -> Ветка -> Расчёт -> Сохранение -> MR.
