# .github/workflows/ai-generate-tests.yml
name: AI Test Generator

on:
  # Вариант 1: По комментарию в PR
  issue_comment:
    types: [created]
  
  # Вариант 2: При создании/обновлении PR (опционально)
  # pull_request:
  #   types: [opened, synchronize]
  
  # Вариант 3: Ручной запуск
  workflow_dispatch:
    inputs:
      file_path:
        description: 'Путь к файлу для генерации тестов (например: src/utils.py)'
        required: true
        type: string
      test_framework:
        description: 'Тестовый фреймворк'
        required: true
        default: 'pytest'
        type: choice
        options:
          - pytest
          - unittest
          - jest
          - vitest
          - go-test

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  # ═══════════════════════════════════════════════════════════
  # JOB 1: По комментарию /generate-tests
  # ═══════════════════════════════════════════════════════════
  generate-from-comment:
    if: |
      github.event_name == 'issue_comment' && 
      github.event.issue.pull_request &&
      contains(github.event.comment.body, '/generate-tests')
    runs-on: ubuntu-latest
    name: Generate Tests (from comment)
    
    steps:
      - name: React to comment
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.reactions.createForIssueComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: context.payload.comment.id,
              content: 'eyes'
            });

      - name: Get PR info
        id: pr
        uses: actions/github-script@v7
        with:
          script: |
            const pr = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number
            });
            core.setOutput('head_ref', pr.data.head.ref);
            core.setOutput('head_sha', pr.data.head.sha);

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ steps.pr.outputs.head_ref }}
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests PyGithub

      - name: Get changed files
        id: files
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr view ${{ github.event.issue.number }} --json files -q '.files[].path' > changed_files.txt
          cat changed_files.txt

      - name: Generate tests
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR_NUMBER: ${{ github.event.issue.number }}
          REPO_NAME: ${{ github.repository }}
          HEAD_REF: ${{ steps.pr.outputs.head_ref }}
          COMMENT_BODY: ${{ github.event.comment.body }}
        run: python .github/scripts/ai_test_generator.py

      - name: React success
        if: success()
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.reactions.createForIssueComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              comment_id: context.payload.comment.id,
              content: 'rocket'
            });

  # ═══════════════════════════════════════════════════════════
  # JOB 2: Ручной запуск
  # ═══════════════════════════════════════════════════════════
  generate-manual:
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    name: Generate Tests (manual)
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests PyGithub

      - name: Generate tests
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO_NAME: ${{ github.repository }}
          TARGET_FILE: ${{ inputs.file_path }}
          TEST_FRAMEWORK: ${{ inputs.test_framework }}
          MODE: manual
        run: python .github/scripts/ai_test_generator.py

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "test: add AI-generated tests for ${{ inputs.file_path }}"
          title: "🧪 AI-generated tests for ${{ inputs.file_path }}"
          body: |
            ## 🤖 Автоматически сгенерированные тесты
            
            **Исходный файл:** `${{ inputs.file_path }}`
            **Фреймворк:** `${{ inputs.test_framework }}`
            
            > ⚠️ Проверьте тесты перед мержем — AI может ошибаться!
          branch: ai-tests/${{ github.run_id }}
          delete-branch: true
